const $=s=>document.querySelector(s);let sensors=[];async function api(path){const r=await fetch(path);if(!r.ok)throw new Error(await r.text());return r.json()}function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function row(s){return `<tr><td>${esc(s.id)}</td><td>${esc(s.name)}</td><td>${esc(s.site_id)}</td><td><span class="status ${esc(s.status)}">${esc(s.status)}</span></td><td>${esc(s.version)}</td><td>${s.last_seen?new Date(s.last_seen).toLocaleString():'—'}</td></tr>`}function row2(s){return `<tr><td>${esc(s.id)}</td><td>${esc(s.name)}</td><td>${esc(s.site_id)}</td><td><span class="status ${esc(s.status)}">${esc(s.status)}</span></td><td>${esc(s.hostname)}</td><td>${s.last_seen?new Date(s.last_seen).toLocaleString():'—'}</td></tr>`}async function refresh(){try{sensors=await api('/v1/sensors');const on=sensors.filter(x=>x.status==='online').length;$('#online').textContent=on;$('#offline').textContent=sensors.length-on;$('#total').textContent=sensors.length;$('#sensorRows').innerHTML=sensors.map(row).join('')||'<tr><td colspan="6">No sensors registered</td></tr>';$('#sensorRows2').innerHTML=sensors.map(row2).join('')||'<tr><td colspan="6">No sensors registered</td></tr>'}catch(e){$('#sensorRows').innerHTML=`<tr><td colspan="6">Unable to load sensors: ${esc(e.message)}</td></tr>`}}document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.view').forEach(v=>v.classList.add('hidden'));$('#'+b.dataset.view).classList.remove('hidden')});refresh();setInterval(refresh,10000)